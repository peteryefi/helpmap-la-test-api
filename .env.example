APP_ENV=production
DATABASE_URL=sqlite:///./data/reports.db

# Set this to the real Amplify URL (and custom domain, if any) once the
CORS_ORIGINS=*

RATE_LIMIT_SUBMIT=15/minute
RATE_LIMIT_LIST=60/minute
